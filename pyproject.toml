```toml
[project]
name = "health-simplified-cli"
version = "0.1.0"
dependencies = [
    "sqlalchemy>=2.0.0",
    "typer>=0.9.0",
    "pytest>=7.0.0",
    "pytest-asyncio>=0.21.0"
]
requires-python = ">=3.11"

[project.scripts]
myapp = "health_tracker.cli:app"
```